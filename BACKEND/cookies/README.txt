# Cookie session file — auto-generated saat warmup berhasil
# File ini menyimpan cookies Cloudflare (cf_clearance) untuk persistent session
# JANGAN commit ke Git! (.gitignore sudah exclude folder ini)
